diff a/lib/widget/dev_tools_sheet.dart b/lib/widget/dev_tools_sheet.dart	(rejected hunks)
@@ -10,7 +12,6 @@ import '../theme/app_theme.dart';
 
 // ÔöÇÔöÇ Panel God Mode (Dev Tools Sheet) ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
 // Fitur khusus developer untuk memodifikasi stat akun secara instan.
-// Bisa menambah/mengurangi Gold, XP, HP, reset inventory, dan demo notifikasi.
 class DevToolsSheet extends StatefulWidget {
   final Map<String, dynamic> userData;
   final VoidCallback onUpdated;
@@ -184,363 +335,164 @@ class _DevToolsSheetState extends State<DevToolsSheet> {
       ),
       child: Column(
         children: [
-          Text(
-            rank,
-            style: GoogleFonts.nunito(
-              color: color,
-              fontSize: 18,
-              fontWeight: FontWeight.bold,
-            ),
-          ),
-          Text(
-            'Lv.$lv',
-            style: GoogleFonts.nunito(
-              color: color.withValues(alpha: 0.7),
-              fontSize: 12,
-            ),
-          ),
+          Text(rank, style: GoogleFonts.nunito(color: color, fontSize: 18, fontWeight: FontWeight.bold)),
+          Text('Lv.$lv', style: GoogleFonts.nunito(color: color.withValues(alpha: 0.7), fontSize: 12)),
         ],
       ),
     );
   }
 
-  Future<void> _addGold(int amount) async {
-    final uid = FirebaseAuth.instance.currentUser?.uid;
-    if (uid == null) return;
-    final currentGold = int.tryParse(_goldCtrl.text) ?? 0;
-    final newGold = currentGold + amount;
-
-    await FirebaseFirestore.instance.collection('users').doc(uid).update({
-      UserSchema.gold: newGold,
-    });
-
-    if (mounted) {
-      setState(() {
-        _goldCtrl.text = newGold.toString();
-      });
-      AudioService.playClick();
-      widget.onUpdated();
-      ScaffoldMessenger.of(context).showSnackBar(
-        SnackBar(
-          content: Text('Added $amount Gold! ­ƒÆ░'),
-          duration: const Duration(milliseconds: 500),
-          backgroundColor: Colors.amber,
-        ),
-      );
-    }
-  }
+  // ÔöÇÔöÇ Level ┬▒1 ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
 
-  Future<void> _addXP(int amount) async {
-    final uid = FirebaseAuth.instance.currentUser?.uid;
+  Future<void> _levelUp() async {
+    final uid = await _getTargetUid();
     if (uid == null) return;
-
-    final currentXP = int.tryParse(_xpCtrl.text) ?? 0;
-    final newXP = currentXP + amount;
     final currentLv = int.tryParse(_levelCtrl.text) ?? 1;
-
-    // Simple level up logic for God Mode:
-    // Every 100 XP added via this button can trigger a level up check if desired,
-    // but let's make it follow the _xpNext logic.
-    int xpNeeded = 100 + (currentLv - 1) * 50;
-    int newLv = currentLv;
-    int tempXP = newXP;
-
-    // Calculate new level based on cumulative XP logic
-    // (Assuming level up happens when XP >= threshold for current level)
-    if (tempXP >= xpNeeded) {
-      newLv++;
-    }
-
+    final newLv = currentLv + 1;
     await FirebaseFirestore.instance.collection('users').doc(uid).update({
-      UserSchema.xp: newXP,
       UserSchema.level: newLv,
     });
-
-    if (mounted) {
-      setState(() {
-        _xpCtrl.text = newXP.toString();
-        _levelCtrl.text = newLv.toString();
-      });
-
-      if (newLv > currentLv) {
-        AudioService.playLevelUp();
-        final oldRank = RankSystem.rankFromLevel(currentLv);
-        final newRank = RankSystem.rankFromLevel(newLv);
-        _showLevelUpPopup(currentLv, newLv, oldRank, newRank);
-      } else {
-        AudioService.playClick();
-      }
-
-      widget.onUpdated();
-      ScaffoldMessenger.of(context).showSnackBar(
-        SnackBar(
-          content: Text(
-            'Added $amount XP! ${newLv > currentLv ? 'LEVEL UP!' : ''}',
-          ),
-          duration: const Duration(milliseconds: 500),
-          backgroundColor: Colors.blueAccent,
-        ),
-      );
-    }
-  }
-
-  Future<void> _resetXP() async {
-    final uid = FirebaseAuth.instance.currentUser?.uid;
-    if (uid == null) return;
-    await FirebaseFirestore.instance.collection('users').doc(uid).update({
-      UserSchema.xp: 0,
+    setState(() {
+      _levelCtrl.text = newLv.toString();
+      _selectedTier = RankSystem.rankFromLevel(newLv);
     });
-    if (mounted) {
-      setState(() {
-        _xpCtrl.text = '0';
-      });
-      AudioService.playClick();
-      widget.onUpdated();
-      ScaffoldMessenger.of(context).showSnackBar(
-        const SnackBar(
-          content: Text('XP Reset to 0!'),
-          duration: Duration(milliseconds: 800),
-          backgroundColor: Colors.orangeAccent,
-        ),
-      );
-    }
+    AudioService.playLevelUp();
+    widget.onUpdated();
   }
 
-  Future<void> _forceLevelUp() async {
-    final uid = FirebaseAuth.instance.currentUser?.uid;
+  Future<void> _levelDown() async {
+    final uid = await _getTargetUid();
     if (uid == null) return;
     final currentLv = int.tryParse(_levelCtrl.text) ?? 1;
-    final newLv = currentLv + 1;
-
+    if (currentLv <= 1) return;
+    final newLv = currentLv - 1;
     await FirebaseFirestore.instance.collection('users').doc(uid).update({
       UserSchema.level: newLv,
-      UserSchema.hp: 100,
     });
-
-    if (mounted) {
-      setState(() {
-        _levelCtrl.text = newLv.toString();
-      });
-      AudioService.playLevelUp();
-      widget.onUpdated();
-      ScaffoldMessenger.of(context).showSnackBar(
-        const SnackBar(
-          content: Text('Level UP! ­ƒÜÇ'),
-          duration: Duration(milliseconds: 800),
-          backgroundColor: Colors.purpleAccent,
-        ),
-      );
-    }
+    setState(() {
+      _levelCtrl.text = newLv.toString();
+      _selectedTier = RankSystem.rankFromLevel(newLv);
+    });
+    widget.onUpdated();
   }
 
-  Future<void> _resetLevel() async {
-    final uid = FirebaseAuth.instance.currentUser?.uid;
+  // ÔöÇÔöÇ Reset XP ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+
+  Future<void> _resetXP() async {
+    final uid = await _getTargetUid();
     if (uid == null) return;
     await FirebaseFirestore.instance.collection('users').doc(uid).update({
-      UserSchema.level: 1,
       UserSchema.xp: 0,
-      UserSchema.hp: 100,
     });
-
-    if (mounted) {
-      setState(() {
-        _levelCtrl.text = '1';
-        _xpCtrl.text = '0';
-      });
-      AudioService.playClick();
-      widget.onUpdated();
-      ScaffoldMessenger.of(context).showSnackBar(
-        const SnackBar(
-          content: Text('Level & XP Reset!'),
-          duration: Duration(milliseconds: 800),
-          backgroundColor: Colors.redAccent,
-        ),
-      );
-    }
+    setState(() => _xpCtrl.text = '0');
+    widget.onUpdated();
+    _showSuccessSnackbar('XP Reset ke 0!');
   }
 
-  Future<void> _reduceHP(int amount) async {
-    final uid = FirebaseAuth.instance.currentUser?.uid;
+  // ÔöÇÔöÇ HP ┬▒20 ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+
+  Future<void> _changeHP(int delta) async {
+    final uid = await _getTargetUid();
     if (uid == null) return;
+    final currentHp = widget.userData[UserSchema.hp] ?? 100;
+    final maxHp = widget.userData[UserSchema.maxHp] ?? 100;
+    final newHp = (currentHp + delta).clamp(0, maxHp);
     await FirebaseFirestore.instance.collection('users').doc(uid).update({
-      UserSchema.hp: FieldValue.increment(-amount),
+      UserSchema.hp: newHp,
     });
-    AudioService.playFail();
+    if (delta < 0) AudioService.playFail();
     widget.onUpdated();
-    if (mounted) {
-      ScaffoldMessenger.of(context).showSnackBar(
-        SnackBar(
-          content: Text('HP Reduced by $amount! ­ƒÆö'),
-          duration: const Duration(milliseconds: 800),
-          backgroundColor: Colors.red,
-        ),
-      );
-    }
+    _showSuccessSnackbar(delta > 0 ? 'HP +$delta ÔåÆ $newHp/$maxHp ÔØñ´©Å' : 'HP $delta ÔåÆ $newHp/$maxHp ­ƒÆö');
   }
 
+  // ÔöÇÔöÇ Demo Tutorial ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+
   Future<void> _demoTutorial() async {
-    final uid = FirebaseAuth.instance.currentUser?.uid;
+    final uid = await _getTargetUid();
     if (uid == null) return;
-    
     await FirebaseFirestore.instance.collection('users').doc(uid).update({
       'tutorialsCompleted': {
-        'home': false,
-        'daily': false,
-        'stats': false,
-        'profile': false,
-        'addTask': false,
-        'leaderboard': false,
+        'home': false, 'daily': false, 'stats': false,
+        'profile': false, 'addTask': false, 'leaderboard': false,
       }
     });
-
     if (mounted) {
-      AudioService.playClick();
       widget.onUpdated();
-      ScaffoldMessenger.of(context).showSnackBar(
-        const SnackBar(
-          content: Text('Panduan xqvx The Creator siap diperagakan! ­ƒÄ¡'),
-          duration: Duration(milliseconds: 1500),
-          backgroundColor: Color(0xFFD4AF37),
-        ),
-      );
-      Navigator.pop(context); // Close dev tools bottom sheet
+      _showSuccessSnackbar('Tutorial siap diperagakan! ­ƒÄ¡');
+      Navigator.pop(context);
     }
   }
 
+  // ÔöÇÔöÇ Test Notification ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+
   Future<void> _testNotification() async {
     if (!mounted) return;
-    
-    AudioService.playClick();
-    
-    // Initialize notification service
     final notificationService = NotificationService();
     await notificationService.initialize();
-    
-    // Show notification selection dialog
+
+    if (!mounted) return;
     showDialog(
       context: context,
       builder: (ctx) => AlertDialog(
         backgroundColor: AppColors.cardBackground,
         shape: RoundedRectangleBorder(
           borderRadius: BorderRadius.circular(20),
-          side: BorderSide(
-            color: Colors.tealAccent.withValues(alpha: 0.5),
-            width: 2,
-          ),
-        ),
-        title: Row(
-          children: [
-            Icon(Icons.notifications_active, color: Colors.tealAccent),
-            SizedBox(width: 12),
-            Text(
-              'Test Notifications',
-              style: GoogleFonts.nunito(
-                color: AppColors.textPrimary,
-                fontSize: 18,
-                fontWeight: FontWeight.bold,
-              ),
-            ),
-          ],
+          side: BorderSide(color: Colors.tealAccent.withValues(alpha: 0.5), width: 2),
         ),
+        title: Row(children: [
+          Icon(Icons.notifications_active, color: Colors.tealAccent),
+          SizedBox(width: 12),
+          Text('Test Notifikasi', style: GoogleFonts.nunito(color: AppColors.textPrimary, fontSize: 18, fontWeight: FontWeight.bold)),
+        ]),
         content: SingleChildScrollView(
           child: Column(
             mainAxisSize: MainAxisSize.min,
             children: [
-              Text(
-                'Pilih notifikasi untuk demo:\n(Akan muncul di notification bar HP)',
-                style: GoogleFonts.nunito(
-                  color: AppColors.textSecondary,
-                  fontSize: 13,
-                ),
-                textAlign: TextAlign.center,
-              ),
-              const SizedBox(height: 16),
-              _buildNotificationOption(
-                ctx,
-                '­ƒÄ» Quest Reminder',
-                'You have 3 pending quests!',
-                Colors.blueAccent,
-                () async {
-                  Navigator.pop(ctx);
-                  await notificationService.showQuestReminder();
-                  _showSuccessSnackbar('Quest Reminder sent! Check notification bar ­ƒöö');
-                },
-              ),
-              _buildNotificationOption(
-                ctx,
-                'ÔÜá´©Å Deadline Alert',
-                'Task deadline in 1 hour!',
-                Colors.orangeAccent,
-                () async {
-                  Navigator.pop(ctx);
-                  await notificationService.showDeadlineAlert();
-                  _showSuccessSnackbar('Deadline Alert sent! Check notification bar ­ƒöö');
-                },
-              ),
-              _buildNotificationOption(
-                ctx,
-                '­ƒöÑ Streak Alert',
-                'Don\'t break your streak!',
-                Colors.redAccent,
-                () async {
-                  Navigator.pop(ctx);
-                  await notificationService.showStreakAlert();
-                  _showSuccessSnackbar('Streak Alert sent! Check notification bar ­ƒöö');
-                },
-              ),
-              _buildNotificationOption(
-                ctx,
-                'Ô¡É Level Up',
-                'You reached Level 10!',
-                Colors.purpleAccent,
-                () async {
-                  Navigator.pop(ctx);
-                  await notificationService.showLevelUp();
-                  _showSuccessSnackbar('Level Up sent! Check notification bar ­ƒöö');
-                },
-              ),
-              _buildNotificationOption(
-                ctx,
-                '­ƒÆ░ Gold Earned',
-                'You earned 50 Gold!',
-                Colors.amber,
-                () async {
-                  Navigator.pop(ctx);
-                  await notificationService.showGoldEarned();
-                  _showSuccessSnackbar('Gold Earned sent! Check notification bar ­ƒöö');
-                },
-              ),
-              _buildNotificationOption(
-                ctx,
-                '­ƒÅå Achievement',
-                'New badge unlocked!',
-                Colors.greenAccent,
-                () async {
-                  Navigator.pop(ctx);
-                  await notificationService.showAchievementUnlocked();
-                  _showSuccessSnackbar('Achievement sent! Check notification bar ­ƒöö');
-                },
-              ),
-              const SizedBox(height: 16),
+              _buildNotifOption(ctx, '­ƒÄ» Quest Reminder', Colors.blueAccent, () async {
+                Navigator.pop(ctx);
+                await notificationService.showQuestReminder();
+                _showSuccessSnackbar('Quest Reminder terkirim! ­ƒöö');
+              }),
+              _buildNotifOption(ctx, 'ÔÜá´©Å Deadline Alert', Colors.orangeAccent, () async {
+                Navigator.pop(ctx);
+                await notificationService.showDeadlineAlert();
+                _showSuccessSnackbar('Deadline Alert terkirim! ­ƒöö');
+              }),
+              _buildNotifOption(ctx, '­ƒöÑ Streak Alert', Colors.redAccent, () async {
+                Navigator.pop(ctx);
+                await notificationService.showStreakAlert();
+                _showSuccessSnackbar('Streak Alert terkirim! ­ƒöö');
+              }),
+              _buildNotifOption(ctx, 'Ô¡É Level Up', Colors.purpleAccent, () async {
+                Navigator.pop(ctx);
+                await notificationService.showLevelUp();
+                _showSuccessSnackbar('Level Up terkirim! ­ƒöö');
+              }),
+              _buildNotifOption(ctx, '­ƒÆ░ Gold Earned', Colors.amber, () async {
+                Navigator.pop(ctx);
+                await notificationService.showGoldEarned();
+                _showSuccessSnackbar('Gold Earned terkirim! ­ƒöö');
+              }),
+              _buildNotifOption(ctx, '­ƒÅå Achievement', Colors.greenAccent, () async {
+                Navigator.pop(ctx);
+                await notificationService.showAchievementUnlocked();
+                _showSuccessSnackbar('Achievement terkirim! ­ƒöö');
+              }),
+              const SizedBox(height: 8),
               SizedBox(
                 width: double.infinity,
                 child: ElevatedButton.icon(
                   onPressed: () async {
                     Navigator.pop(ctx);
-                    _showSuccessSnackbar('Sending all notifications... Check notification bar! ­ƒöö');
+                    _showSuccessSnackbar('Mengirim semua notifikasi... ­ƒöö');
                     await notificationService.showAllDemoNotifications();
                   },
                   icon: Icon(Icons.all_inclusive, size: 18),
-                  label: Text(
-                    'Send All (2s delay)',
-                    style: GoogleFonts.nunito(fontWeight: FontWeight.bold),
-                  ),
+                  label: Text('Kirim Semua', style: GoogleFonts.nunito(fontWeight: FontWeight.bold)),
                   style: ElevatedButton.styleFrom(
                     backgroundColor: Colors.tealAccent,
                     foregroundColor: AppColors.textPrimary,
-                    shape: RoundedRectangleBorder(
-                      borderRadius: BorderRadius.circular(12),
-                    ),
+                    shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
                   ),
                 ),
               ),
